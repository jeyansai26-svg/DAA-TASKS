#include<stdio.h>
int main(){

// Increment i by multiplication
for (int i = 1; i <= n; i *= c) { 
    // some expressions 
}

// Decrement i by division
for (int i = n; i > 0; i /= c) { 
    // some expressions
}
}
